# 作業ルール

このリポジトリで作業する際は、以下のルールに従うこと。

## ブランチ

- `main` に直接コミットしない
- 作業ごとに `main` から切る。命名は `feature/<画面番号>-<英語の短い名前>`
  - 例: `feature/12-profile-close-friend`、`feature/07-calendar-week`
- 作業を始める前に必ず `git switch main && git pull` してから切る

## コミット

- Conventional Commits に従う（`feat:` / `fix:` / `refactor:` / `chore:` / `docs:`）
- 1 行目は **日本語** で 72 文字以内（半角換算目安。type プレフィックスは英語）
- 意味のある単位でこまめに分ける。1 フェーズ 1 コミットにまとめない
  - 例: `feat: 親しい友達 Repository を追加` / `feat: プロフィール画面に親しい友達ボタンを追加`
- 動かない状態でコミットしない。最低限ビルドが通ることを確認する
- 本文（詳細説明）が必要な場合も日本語で可

## コミットしてはいけないもの

- `.env`、API キー、Firebase のサービスアカウント JSON
- ビルド成果物、`.dart_tool`、`build/`、`*.g.dart` 以外の生成物
- コミット前に `git status` と `git diff --staged` を必ず確認する

## プルリクエスト

- 1 フェーズ = 1 PR。`gh pr create` で作成する
- タイトルは日本語で「[画面番号] 内容」の形
  - 例: 「[⑫] 相手プロフィールに親しい友達の追加・解除を追加」
- 本文には次を必ず含める
  1. 何をしたか（箇条書き）
  2. 動作確認した内容
  3. バックエンドへの依存（新しく必要な API、既存 API に追加してほしいフィールド）
  4. 未対応・TODO
- サーバー側が未実装でモックで動かしている箇所は、PR 本文に明記する

## やらないこと

- 勝手に force push しない
- 勝手にブランチを削除しない
- コミットメッセージに Claude や AI に関する署名を入れない
